require("toggleterm").setup({
    size = 12,
    open_mapping = [[<C-/>]],
    direction = "horizontal",
    close_on_exit = false, -- Maybe true
    shade_terminals = true,
    start_in_insert = true,
    persist_mode = true,
})
